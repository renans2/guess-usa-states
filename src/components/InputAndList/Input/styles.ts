import styled from "styled-components";
import { S_Surface } from "../../../ui/surface";

export const S_InputContainer = styled(S_Surface).attrs({ as: "input"})`
  width: 100%;
  padding: 10px 14px;
  font-size: 1.5rem;
`;